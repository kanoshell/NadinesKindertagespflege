<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.daheim.nadineskindertagespflege">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".neuesKind"
            android:label="Daten eingeben" />
        <activity
            android:name=".Kinder"
            android:label="Kinder" />
        <activity android:name=".anzeigeKind" />
        <activity
            android:name=".DriveFileManagement"
            android:label="@string/title_activity_drive_file_management"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Anwesenheit"
            android:label="@string/title_activity_anwesenheit"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>